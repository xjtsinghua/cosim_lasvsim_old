
from .config import (
    VehicleSpec as VehicleSpec,
    CCBFOption as CCBFOption,
    # Dimension
    state_dim as state_dim,
    control_dim as control_dim,
    sur_dim as sur_dim,
    reference_dim as reference_dim,
)
